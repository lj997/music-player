<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.musicplayer.mapper.PlayHistoryMapper">
    <resultMap id="PlayHistoryResultMap" type="com.musicplayer.entity.PlayHistory">
        <id column="id" property="id"/>
        <result column="song_id" property="songId"/>
        <result column="play_time" property="playTime"/>
    </resultMap>

    <select id="selectRecent" resultMap="com.musicplayer.mapper.SongMapper.SongResultMap">
        SELECT DISTINCT s.* FROM song s
        INNER JOIN play_history ph ON s.id = ph.song_id
        ORDER BY ph.play_time DESC
        LIMIT #{limit}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM play_history
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO play_history (song_id, play_time)
        VALUES (#{songId}, NOW())
    </insert>

    <delete id="deleteOldest">
        DELETE FROM play_history 
        WHERE id NOT IN (
            SELECT id FROM (
                SELECT id FROM play_history ORDER BY play_time DESC LIMIT #{limit}
            ) AS temp
        )
    </delete>
</mapper>