<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.musicplayer.mapper.WallpaperMapper">
    <resultMap id="WallpaperResultMap" type="com.musicplayer.entity.Wallpaper">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="file_path" property="filePath"/>
        <result column="is_current" property="isCurrent"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectById" resultMap="WallpaperResultMap">
        SELECT * FROM wallpaper WHERE id = #{id}
    </select>

    <select id="selectCurrent" resultMap="WallpaperResultMap">
        SELECT * FROM wallpaper WHERE is_current = 1 LIMIT 1
    </select>

    <select id="selectAll" resultMap="WallpaperResultMap">
        SELECT * FROM wallpaper ORDER BY is_current DESC, create_time DESC
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM wallpaper
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wallpaper (name, file_path, is_current)
        VALUES (#{name}, #{filePath}, #{isCurrent})
    </insert>

    <update id="updateCurrent">
        UPDATE wallpaper SET is_current = 1 WHERE id = #{id}
    </update>

    <update id="resetCurrent">
        UPDATE wallpaper SET is_current = 0
    </update>

    <delete id="deleteById">
        DELETE FROM wallpaper WHERE id = #{id}
    </delete>
</mapper>